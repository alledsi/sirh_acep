# SIRH ACEP — Dépendances
# Python >= 3.10 (testé sur 3.10, 3.11, 3.12). Ubuntu 24.04 livre Python 3.12.

Django>=5.1,<6.0
django-environ>=0.11

# Driver PostgreSQL (utilisé en prod, optionnel en dev avec SQLite)
psycopg[binary]>=3.1

# Serveur d'application
gunicorn>=21.2

# Gestion des fichiers statiques en production
whitenoise>=6.6

# Upload d'images (photos employés)
Pillow>=10.2

# Lecture / écriture des fichiers Excel (.xlsx) — import employés + export stats
openpyxl>=3.1

# Dev tools (optionnel — installe avec : pip install -r requirements-dev.txt)
# django-debug-toolbar>=4.3
# ruff>=0.4
# black>=24.3
